import java.util.ArrayList;

public class Integers {
ArrayList <Integer > nums = new ArrayList();

static int computeSum(ArrayList<Integer> nyork) {
	int sum = 0;
	 for(int num : nyork) {
		 sum+= num;
	 }
	 return sum;
	
}
}
